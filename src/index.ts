/**
 * glow-card-react — public entry point.
 *
 * The implementation (the `useGlowCard` hook and `<GlowCard>` component)
 * lands in a follow-up commit. This placeholder keeps the package importable
 * while the scaffold is in place.
 */
export {};
